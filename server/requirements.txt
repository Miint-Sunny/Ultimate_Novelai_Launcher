fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
pydantic>=2.0.0
httpx[socks]>=0.23.0   # [socks] = 装 socksio 子依赖，PROXY_URL=socks5:// 时必须
curl_cffi>=0.7.0
pynacl>=1.5.0       # Boost 通道：NovelAI access_key 派生（Argon2id）
# Agent 框架已自研内置（agent_router/llm，仅依赖 pydantic + httpx，二者已在上方），不再需要 pydantic-ai。
Pillow>=10.0.0
gradio_client>=1.12.1
