// 角色识别工具：实现已统一到 src/utils/characterRecognition.ts（桌面/移动共用）。
// 保留此模块路径作为移动端的兼容入口，导出形状不变。
export { useCharacterRecognition } from '../../../utils/characterRecognition';
export type { CharacterMatch } from '../../../utils/characterRecognition';
